# Index Folder

Folder ini berisi file-file yang dihasilkan oleh ingestion pipeline:

- `faiss.index` — FAISS vector index
- `metadata.json` — Metadata chunk (teks, halaman, section)

File-file ini di-generate otomatis oleh `python ingestion/build_index.py`.
